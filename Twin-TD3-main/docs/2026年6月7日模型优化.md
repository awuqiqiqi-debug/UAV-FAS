# 模型优化记录

## 优化概述

基于代码实际运行状态，全面修正文档与代码的不一致之处。主要修正了发射天线架构、端口选择机制、动作空间维度、状态空间维度、移动模型、奖励函数设计等内容。

---

## 一、核心架构变更

| 序号 | 优化项 | 旧版本 | 当前代码实际值 | 涉及文件 |
|------|--------|--------|--------------|----------|
| 1 | 发射天线 | BS(4天线) + FAS(12端口) | **仅 FAS(12端口)** — FAS 作为唯一发射天线 | src/envs/uav_comm_env.py |
| 2 | FAS 增益 | 复数矩阵 (12×1) | **标量** F ∈ [1, 3]，多端口等功率分配 | src/envs/uav_comm_env.py |
| 3 | FAS 端口选择 | 启发式 + RL微调 → RL argmax | **Gumbel-Softmax Top-K** 同时激活2~3个端口 | src/envs/uav_comm_env.py |
| 4 | Agent 1 动作空间 | 48维 → 38维 | **39维** (12端口+1F增益+1β+1η+24相位) | src/envs/uav_comm_env.py |
| 5 | Agent 1 状态空间 | 42维 → 89维 | **89维** (72端口信道+3位置+14系统) | src/envs/uav_comm_env.py |
| 6 | Agent 2 动作空间 | 3维 (Δx,Δy,Δz) | **2维** (vx, vy) — 高度固定50m | src/main_train.py |
| 7 | Agent 2 状态空间 | 9维 → 18维 | **18维** (含RIS/窃听者位置+信道容量) | src/envs/uav_comm_env.py |
| 8 | 移动模型 | 位移控制 (D_max=0.25m) | **速度控制** V_MAX=1.0m/s, DT=0.1s | src/envs/uav_comm_env.py |
| 9 | 飞行边界 | X:[-50,50], Y:[-20,60] | **X:[-50,50], Y:[-50,50]** | src/envs/uav_comm_env.py |
| 10 | 奖励函数 | 位置+容量+窃听多组件 | **多组件加权** 保密速率+FAS增益+约束惩罚 | src/envs/uav_comm_env.py |
| 11 | RIS 放大 | 无有源放大 | **有源放大** β ∈ [1, √11]，Agent控制干扰比例η | src/envs/uav_comm_env.py |
| 12 | RIS 位置 | (0, 50, 12.5) | **(20, -20, 12.5)** — 靠近窃听者 | src/envs/uav_comm_env.py |
| 13 | RIS 功率约束 | Pr=0dBm | **Pr=30dBm, P_J=20dBm** | src/envs/uav_comm_env.py |
| 14 | 干扰比例 | 自适应（信道比决定） | **Agent控制** η ∈ [0.1, 0.6] + 课程学习对准 | src/envs/uav_comm_env.py |
| 15 | 默认训练轮数 | 350 | **2000** | src/main_train.py |
| 16 | 实体命名 | UAV_FAS | **UAV_FAS** (移除BS引用) | src/envs/entity.py, uav_comm_env.py |

---

## 二、FAS 端口选择机制

### 2.1 Gumbel-Softmax Top-K（当前）

RL 输出 12 维 logits，通过 Gumbel-Softmax + Top-K 同时激活多个端口：

```python
# src/envs/uav_comm_env.py — step()
port_logits_tensor = torch.tensor(G[:self.UAV_FAS.fas_num_ports], dtype=torch.float)
if self.training:
    # 训练时: Gumbel-Softmax近似argmax，可微分
    port_probs = F.gumbel_softmax(port_logits_tensor, tau=0.5, hard=False)
    _, topk_indices = torch.topk(port_probs, self.num_active_ports)
else:
    # 推理时: 硬选择Top-K
    _, topk_indices = torch.topk(port_logits_tensor, self.num_active_ports)

self.UAV_FAS.fas_active_ports = topk_indices.numpy().tolist()
self.UAV_FAS.fas_active_port = self.UAV_FAS.fas_active_ports[0]  # 主端口(兼容)
```

**关键参数**：
- `num_active_ports`：同时激活的端口数（默认2，可设为2~3）
- `tau=0.5`：Gumbel-Softmax 温度参数

### 2.2 FAS 功率分配

K 个端口等功率分配，总功率受硬约束：

```python
F_total = 1.0 + (G[self.UAV_FAS.fas_num_ports] + 1.0) / 2.0 * 2.0  # [1, 3]
P_max_linear = 10 ** (P_max_dBm / 10)  # 1000 mW
if F_total ** 2 > P_max_linear:
    F_total = np.sqrt(P_max_linear)
self.UAV_FAS.F = F_total / math.sqrt(self.num_active_ports)  # 每端口功率 = F_total² / K
```

---

## 三、动作空间详细说明

### 3.1 Agent 1 (FAS + RIS) — 39 维

| 参数范围 | 维度 | 含义 |
|---------|------|------|
| action_1[0:12] | 12 | FAS 端口选择 (Gumbel-Softmax logits) |
| action_1[12:13] | 1 | FAS 增益 F ∈ [1, 3] |
| action_1[13:14] | 1 | RIS 放大增益 β ∈ [1, √11] |
| action_1[14:15] | 1 | RIS 干扰比例 η ∈ [0.1, 0.6] ← Agent控制 |
| action_1[15:39] | 24 | RIS 相位 (12信号反射 + 12人工噪声) |

**代码实现**：

```python
# src/envs/uav_comm_env.py — get_system_action_dim()
def get_system_action_dim(self):
    result = 0
    if self.if_with_FAS:
        result += self.UAV_FAS.fas_num_ports  # 端口选择: 12维 (Gumbel-Softmax)
        result += 1      # FAS增益 F: 1维
        result += 1      # RIS放大增益 β: 1维
        result += 1      # RIS干扰比例 η ∈ [0,1]: 1维 ← Agent控制
        result += 2 * 12  # RIS相位: 24维 (12信号+12干扰)
    return result  # = 39
```

### 3.2 Agent 2 (UAV 轨迹) — 2 维

| 参数范围 | 维度 | 含义 |
|---------|------|------|
| action_2[0] | 1 | vx 水平速度 ∈ [-1, 1] |
| action_2[1] | 1 | vy 水平速度 ∈ [-1, 1] |

---

## 四、RIS 干扰比例 — Agent控制 + 课程学习

### 4.1 Agent 控制干扰比例 η

```python
jam_ratio_raw = float(Phi[1]) if len(Phi) > 1 else 0.0
jam_ratio = 0.1 + (jam_ratio_raw + 1.0) / 2.0 * 0.5  # [0.1, 0.6]
jam_elements = int(self.RIS.ant_num * jam_ratio)
reflect_elements = self.RIS.ant_num - jam_elements
```

### 4.2 课程学习对准

初始阶段对准窃听者80%，逐渐衰减到0%，让 Agent 逐步自主学习：

```python
# 初始化参数
self.jam_align_weight = 0.8    # 初始对准权重
self.jam_align_decay = 0.995   # 每episode衰减因子
self.jam_align_min = 0.0       # 最小对准权重

# 混合Agent相位 + 对准相位
eve_channel_phase = np.angle(h_R_p_arr * h_UR_active)
jam_agent_phase = ...   # Agent输出
jam_align_phase = -eve_channel_phase  # 对准窃听者
jam_mixed_phase = (1 - self.jam_align_weight) * jam_agent_phase + self.jam_align_weight * jam_align_phase
```

---

## 五、RIS 位置变更

**旧版**：RIS 位于 (0, 50, 12.5) — 远离窃听者
**当前**：RIS 位于 (20, -20, 12.5) — 靠近窃听者 (47, -4, 0)

```python
# src/envs/uav_comm_env.py
ris_coordinate = np.array([20, -20, 12.5])  # RIS固定位置（靠近窃听者）
self.RIS = RIS(coordinate=ris_coordinate, coor_sys_z=ris_coor_sys_z, ant_num=ris_ant_num,
               Pr=30, P_J=20, beta=10, sigma=-100)  # Pr=30dBm, P_J=20dBm
```

---

## 六、文件对应关系

| 文件 | 内容 | 关键类/函数 |
|------|------|-----------|
| `src/envs/entity.py` | 实体定义（UAV、RIS、User、Attacker） | `UAV_FAS`, `RIS`, `User`, `Attacker` |
| `src/envs/uav_comm_env.py` | 环境模型（状态、动作、奖励、容量） | `MiniSystem` |
| `src/envs/channel.py` | 毫米波 LoS 信道模型 | `mmWave_channel` |
| `src/envs/math_tool.py` | 工具函数 | `dB_to_normal`, `convert_list_to_complex_matrix` |
| `src/networks/actor_critic.py` | 神经网络（Actor-Critic） | `ActorNetwork`, `CriticNetwork` |
| `src/agents/td3_agent.py` | Twin-TD3 智能体 | `Agent` |
| `src/agents/ddpg_agent.py` | DDPG 智能体 | `Agent` |
| `src/agents/sac_agent.py` | SAC 智能体 | `SACAgent` |
| `src/main_train.py` | TD3 训练主脚本 | 训练循环 |
| `src/main_train_sac.py` | SAC 训练脚本 | SAC 训练循环 |
| `src/main_train_td3_velocity.py` | 速度约束 TD3 训练 | 速度→位移转换 |
| `src/utils/data_manager.py` | 数据管理 | `DataManager` |
| `src/utils/renderer.py` | 3D 可视化 | `Render` |
